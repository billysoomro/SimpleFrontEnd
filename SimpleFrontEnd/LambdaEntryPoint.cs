using Amazon.Lambda.AspNetCoreServer;

namespace SimpleFrontEnd;
public class LambdaEntryPoint : APIGatewayProxyFunction
{
    protected override void Init(IWebHostBuilder builder)
    {
        builder.UseStartup<Startup>();
    }
}
